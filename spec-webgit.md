We have built the core of the notebooking app now.
The next thing to do is make a way to publish and share notebooks.

Here's what I want to do.
For my current open notebook.
I want to have a button 

git remote add origin https://github.com/liamzebedee/bio-notebook.git
git branch -M main
git push -u origin main
